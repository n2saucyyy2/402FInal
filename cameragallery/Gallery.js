import React from 'react';
import imageList from './imageList';

//import imageGrid from 'imageGrid';
//import imageScroll from 'imageScroll';


const GalleryView = ({ item,index }) => {
   var curview = <imageList />;

    return (curview);
};

export default GalleryView